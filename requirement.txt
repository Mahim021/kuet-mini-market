First Read this requirement.txt file and database-schema.md files first(both are in the same project root folder). Then do ONLY Dockerization for this existing KUET Mini Market Spring Boot project.

Important context:
- This is an existing Spring Boot project
- Security/authentication work from feature/security has already been completed and merged into develop
- The current project already includes user-related tables and security foundation
- Do NOT rewrite or disturb existing authentication, JWT, security config, tests, DTOs, services, or controllers unless a very small change is absolutely required for Docker compatibility
- Your task is ONLY to dockerize the existing project cleanly and professionally

Important database requirement:
- We must use PostgreSQL database
- Do NOT use H2 as the runtime application database
- H2 may remain only for tests if already present
- Docker Compose must run the Spring Boot app with PostgreSQL

Database schema to respect:
- users
- roles
- user_roles
- products
- orders
- order_items


Database name:
mini-market

Your task:
Dockerize this existing project so that the Spring Boot application and PostgreSQL database can run together using Docker Compose.

Strict scope:
Do ONLY these Docker-related tasks:
1. Create a proper Dockerfile for the Spring Boot application
2. Create docker-compose.yml with:
   - app service
   - db service (PostgreSQL)
3. Configure database connection using environment variables:
   - SPRING_DATASOURCE_URL
   - SPRING_DATASOURCE_USERNAME
   - SPRING_DATASOURCE_PASSWORD
4. Ensure the app runs against PostgreSQL inside Docker
5. Make sure the project can run with:
   docker compose up --build
6. Keep the setup compatible with the current codebase
7. Do not break existing local testing setup
8. If needed, create:
   - .dockerignore
   - application-docker.properties
9. If configuration changes are needed, use a clean profile-based approach
10. Keep the solution simple, stable, and appropriate for a student full-stack project
11. Preserve current project structure and do not redesign business logic
12. If needed, add PostgreSQL initialization support
13. Ensure service naming and connection string work correctly inside Docker network
14. If needed, make the app wait properly for PostgreSQL startup

Important constraints:
- Do NOT implement products module
- Do NOT implement orders module
- Do NOT implement cart module
- Do NOT implement deployment
- Do NOT change API design
- Do NOT rewrite security/authentication logic
- Do NOT remove H2-based test configuration if present
- Do NOT change CI workflow unless a very small Docker-related adjustment is absolutely necessary
- Do NOT add unnecessary files

Expected output:
1. First inspect the current project carefully
2. List all files you will create or update
3. Then provide complete code for each file
4. If an existing file must be modified, provide the full updated file
5. Include all necessary Docker-related files only
6. Briefly explain how to run the project using Docker
7. Keep everything compilable and consistent

Technical target:
After your changes, the project should run successfully with:
docker compose up --build

Runtime requirement:
- The application must use PostgreSQL as the main runtime database

Now inspect the project carefully and generate the full Dockerization setup only.